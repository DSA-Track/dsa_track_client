import 'package:flutter/material.dart';

class Q_MiddleColumn extends StatelessWidget {
  const Q_MiddleColumn({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}