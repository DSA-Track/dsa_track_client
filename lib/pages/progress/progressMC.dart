import 'package:flutter/material.dart';

class ProgressMiddleColumn extends StatelessWidget {
  const ProgressMiddleColumn({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}